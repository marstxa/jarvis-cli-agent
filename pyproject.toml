[project]
name = "jarvis-cli-agent"
version = "0.1.0"
description = "A minimal, educational tool-calling coding agent built on the Gemini API"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv",
]

[tool.setuptools.packages.find]
include = ["jarvis*"]